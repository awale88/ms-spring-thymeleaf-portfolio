#Application
spring.application.name=portfolio

#Server
server.port=${PORT:8080}

#Email SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USER}
spring.mail.password=${EMAIL_PASS}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#Database - PostgresDB
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}

#JPA
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Enable Swagger UI
springdoc.swagger-ui.enabled=false
springdoc.swagger-ui.path=${SWAGGER_URL}/swagger-ui.html

# API Documentation Info
springdoc.api-docs.path=${SWAGGER_URL}/api-docs
springdoc.swagger-ui.config-url=${SWAGGER_URL}/api-docs/swagger-config
springdoc.swagger-ui.url=${SWAGGER_URL}/api-docs

# Customize Swagger UI
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.operationsSorter=alpha

# For Railway environment
springdoc.swagger-ui.disable-swagger-default-url=true